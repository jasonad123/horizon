services:
  horizon-dev:
    image: node:24-alpine@sha256:a0b9bf06e4e6193cf7a0f58816cc935ff8c2a908f81e6f1a95432d679c54fbfd
    container_name: horizon-dev-frontend
    working_dir: /app/svelte-app
    restart: 'no'
    ports:
      - '5173:5173'
    environment:
      NODE_ENV: 'development'
      VITE_API_PROXY: 'http://horizon-backend:8080'
    env_file:
      - .env
    volumes:
      - ./svelte-app:/app/svelte-app
      - dev_svelte_modules:/app/svelte-app/node_modules
    networks:
      - horizon-network
    command:
      - sh
      - -c
      - |
        corepack enable
        corepack prepare pnpm@latest --activate
        pnpm install
        pnpm run dev -- --host 0.0.0.0

  horizon-backend:
    image: node:24-alpine@sha256:a0b9bf06e4e6193cf7a0f58816cc935ff8c2a908f81e6f1a95432d679c54fbfd
    container_name: horizon-backend
    working_dir: /app
    restart: 'no'
    ports:
      - '8080:8080'
    environment:
      NODE_ENV: 'development'
    env_file:
      - .env
    volumes:
      - .:/app
      - dev_modules:/app/node_modules
    networks:
      - horizon-network
    command:
      - sh
      - -c
      - |
        corepack enable
        corepack prepare pnpm@latest --activate
        pnpm install
        node --max-old-space-size=400 server/app.js

volumes:
  dev_modules:
  dev_svelte_modules:

networks:
  horizon-network:
    driver: bridge
